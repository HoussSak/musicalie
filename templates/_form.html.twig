{{ form_start(form)}}
    {{ form_widget(form)}}
<input id="btn-create" class="btn btn-primary btn-block" type="submit" value="{{ submitButton | default('Nouveau') }}" formnovalidate>
{{ form_end(form)}}